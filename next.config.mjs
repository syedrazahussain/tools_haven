/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
     //domains: ['localhost'],      // 👈 Allow image loading from localhost

     domains: ['tools-haven-server.onrender.com'],
  },
};

export default nextConfig;
